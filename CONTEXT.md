# knowledge-viz

生物概念可视化讲解站点。本文件是领域术语表，只收本项目特有的概念，不含实现细节。

## Language

### 课程结构

**课程（Course）**：
一个可讲解的知识点，由阶段序列和配套图表组成。
_Avoid_: 项目、模块（指代码目录时除外）

**模式（Mode）**：
同一课程模块承载的平行叙事变体（如减数分裂课程的精子形成/卵细胞形成），靠路由参数切换，切换即重置到第 0 阶段。
_Avoid_: 课程（模式不是独立课程）

**注册表（Registry）**：
main.ts 中课程元数据与加载器的登记处，新增知识点的唯一入口。
_Avoid_: 白名单、配置

### 场景与动画

**阶段（Stage）**：
动画的信息单元，等于播放器的一个暂停节点。「一帧一信息」实为「一阶段一信息」：暂停到任意阶段，画面本身能答出正在发生什么。
_Avoid_: 帧（补间中间态不是阶段）、步骤、节点

**元素（Element）**：
场景中拥有独立位置/角度/透明度的最小 SVG 插值单元。一根单体杆、一个着丝点、一条基因标注各算一个元素。
_Avoid_: 图形、对象、控件（控件指交互部件，不是元素）

**单体杆（Chromatid rod）**：
表示一条染色单体的圆角短杆，是场景的主体元素。
_Avoid_: 染色体棒、bar

**槽位（Slot）**：
某元素在某阶段的目标几何（位置+角度），以相对所属细胞中心的偏移表达；卵细胞后期阶段例外地用画布绝对坐标。
_Avoid_: 坐标（笼统）、布局（布局是全部槽位的整体）

**槽位表（Slots）**：
单个阶段全部元素的槽位声明集合，纯数据，逐阶段显式给出。
_Avoid_: 布局函数（函数只是承载声明的方式）

**布局不变式（Layout invariant）**：
任何阶段的槽位都必须满足的可读性下限（如异染色体最小间距），由测试断言强制。
_Avoid_: 规则（泛称）

**示意取舍（Schematic license）**：
有意偏离真实生物学结构的简化决定（隐藏、夸大、合并）。每个取舍必须登记，防止后人当缺陷修复。
_Avoid_: bug、偷懒

### 基因表达（分子生物学课程）

**模板链（Template strand）**：
转录中被 RNA 聚合酶读取的 DNA 单链；同一基因只读一条，不同基因可用不同链。本课程约定置于画面下行。
_Avoid_: 反义链（超纲）、下链（位置描述非角色）

**密码子（Codon）**：
mRNA 上每 3 个相邻碱基构成的氨基酸编码单元，连续阅读、不重叠。本课程用组界线视觉分组。
_Avoid_: 三联体（笼统）、分组线（那是画面元素）

**反密码子（Anticodon）**：
tRNA 上与 mRNA 密码子互补配对的三碱基，位于 tRNA 图形底部。
_Avoid_: 底部字母（实现描述）

**转录泡（Transcription bubble）**：
转录期间 DNA 双链局部解开的区域，随聚合酶移动。本课程以阶段快照呈现（位置在幕间跳变、不连续滑动）。
_Avoid_: 解旋区（易与 DNA 复制解旋混淆）

**P/A 位点（P/A site）**：
核糖体上两个 tRNA 结合位：P 位持肽链、A 位接受进位。E 位（出口位）被有意裁剪；空载 tRNA 以离场动画表示离开（下坠远离 mRNA 并半透明，下一帧退场）。
_Avoid_: 左位/右位（方位描述非语义）、就地淡出（无方向感的旧做法）

### 验收

**目检（Visual review）**：
无法用断言表达的直观性/语义类准则，由用户人工查看验收；agent 侧只能提交待目检清单。
_Avoid_: 自测（含混）、看一眼
